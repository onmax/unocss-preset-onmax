# Onmax's UnoCSS Presets

A collection of UnoCSS that can help you in your projects.

The presets are autamatically added in the `unocss-preset-onmax`, but the presets are also available as a standalone package.
